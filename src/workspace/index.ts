/**
 * 工作区管理模块 (Workspace)
 * 
 * 核心职责：
 * - 整合文件管理、文档编辑、AI聊天三大功能模块
 * - 协调多模块间的数据流和状态同步
 * - 管理多文件选择状态（用于AI对话的上下文增强）
 * 
 * 设计理念：
 * - 作为"协调层"而非"业务层"，不直接处理文件操作
 * - 随着支持格式增多，负责适配不同类型文件的展示策略
 * - 保持轻量，避免重复实现底层功能
 * 
 * 依赖关系：
 * - 使用 file-manager 提供的文件树和操作能力
 * - 使用 document-editor 提供的编辑能力
 * - 使用 chat 提供的AI对话能力
 * - 使用 md-html-dock/utils 提供的文件读取工具
 */
export * from './components'
export * from './hooks'

